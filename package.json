{
  "name": "yield-bet-contract",
  "version": "1.0.0",
  "description": "AO Yield Bet Contract",
  "main": "index.js",
  "scripts": {
    "bundle": "npm run clean && tsx scripts/bundle.ts",
    "clean": "rimraf [ dist ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^6.0.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
